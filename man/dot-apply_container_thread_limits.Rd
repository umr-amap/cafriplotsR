% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/container_resources.R
\name{.apply_container_thread_limits}
\alias{.apply_container_thread_limits}
\title{Size OpenMP thread pools to the container's CPU quota}
\usage{
.apply_container_thread_limits(n = .container_cpu_limit())
}
\arguments{
\item{n}{Integer thread cap. Defaults to the detected container quota.}
}
\value{
Invisibly, the cap applied, or `NA_integer_` if none was.
}
\description{
Caps stringdist and data.table at the number of CPUs the container may use,
and exports `OMP_THREAD_LIMIT` so child processes inherit the same cap: a
`callr` worker is a fresh R session, and stringdist reads that variable when
it loads.
}
\details{
A no-op when no CPU limit is detected, so it is safe to call anywhere; it is
called from the served app entry points and from `.matching_worker()`.
}
\keyword{internal}
