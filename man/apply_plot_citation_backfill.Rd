% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citations_functions.R
\name{apply_plot_citation_backfill}
\alias{apply_plot_citation_backfill}
\title{Apply plot citation backfill from a manually filled data frame}
\usage{
apply_plot_citation_backfill(
  data,
  con = NULL,
  execute = FALSE,
  batch_size = 1000L
)
}
\arguments{
\item{data}{Data frame with at minimum two columns: `id_liste_plots`
(integer, primary key) and `id_citation` (integer, FK to
`table_citations`). Additional columns are ignored.}

\item{con}{Database connection to `plots_transects`. If NULL, calls
`call.mydb()`.}

\item{execute}{Logical. If FALSE (default), shows a preview of what would
be updated without modifying the database.}

\item{batch_size}{Integer. Number of rows per UPDATE batch (default 1000).}
}
\value{
Invisible integer: number of rows updated.
}
\description{
Takes a data frame (typically the output of
`export_plots_for_citation_backfill()` after manual editing) and updates
`id_citation` in `data_liste_plots` for each row where `id_citation` is not
NA. Rows with NA are skipped.
}
\examples{
\dontrun{
con <- call.mydb()

# Export, fill manually, apply
df <- export_plots_for_citation_backfill(con)
# ... fill df$id_citation ...
apply_plot_citation_backfill(df, con = con)              # dry run
apply_plot_citation_backfill(df, con = con, execute = TRUE)
}

}
