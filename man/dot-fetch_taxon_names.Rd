% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/census_taxon_revision.R
\name{.fetch_taxon_names}
\alias{.fetch_taxon_names}
\title{Fetch names and precision for a set of taxon ids}
\usage{
.fetch_taxon_names(idtax, con_taxa)
}
\arguments{
\item{idtax}{Integer vector of `idtax_n` values.}

\item{con_taxa}{Connection or pool for the taxa database.}
}
\value{
Data frame with `idtax_n`, `taxon_name`, `tax_level`, `precision`.
  An empty frame if nothing can be fetched — the caller degrades to ids.
}
\description{
Reads the **taxa** database, which is a separate connection from the one
holding the plots. Showing bare `idtax_n` integers in the review step would
make it unusable, so this is not optional decoration.
}
\keyword{internal}
