% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_census_link.R
\name{.unlink_never_features}
\alias{.unlink_never_features}
\title{Drop the census link from features that do not belong to a census}
\usage{
.unlink_never_features(data, policy, quiet = FALSE)
}
\arguments{
\item{data}{Data frame with `trait_name` and `id_sub_plots` columns.}

\item{policy}{Named character vector as returned by [.feature_census_link()].}

\item{quiet}{Logical. Suppress the message naming the features left out.}
}
\value{
`data`, with `id_sub_plots` cleared where the policy says `"never"`.
}
\description{
Sets `id_sub_plots` back to `NA` on every row whose `trait_name` the policy
calls `"never"`, and says which features were left out. Rows for any other
feature, and rows that carry no census to begin with, are returned
untouched.
}
\keyword{internal}
