% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_feat_step5_validation.R
\name{.plot_labels_for_ids}
\alias{.plot_labels_for_ids}
\title{Readable plot labels for a set of plot ids}
\usage{
.plot_labels_for_ids(con, plot_ids, data = NULL)
}
\arguments{
\item{con}{Database connection, or NULL to skip the database lookup.}

\item{plot_ids}{Vector of `id_liste_plots` values.}

\item{data}{Optional prepared data frame, used first when it holds both
`plot_name` and `id_liste_plots`.}
}
\value{
Named character vector of labels, indexed by plot id as character.
}
\description{
A message about a tag that could not be matched is of little use without the
plot it was looked for in. Names are taken from the prepared data when it
carries them and from the database otherwise; an id that resolves to no name
is labelled `#<id>` rather than left as `NA`.
}
\keyword{internal}
