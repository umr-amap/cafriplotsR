% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_specid_batch_lookup.R
\name{.match_collector_names}
\alias{.match_collector_names}
\title{Match collector names against table_colnam}
\usage{
.match_collector_names(user_values, db_values, db_ids)
}
\arguments{
\item{user_values}{Character vector of user-provided collector strings.}

\item{db_values}{Character vector of `colnam` from `table_colnam`.}

\item{db_ids}{Integer vector of matching `id_table_colnam`.}
}
\value{
list(exact = named list user_value -> id,
             fuzzy = named list user_value -> list(name, id, similarity)
               with ALL candidates ranked by similarity, descending),
             unmatched = character vector (only values that failed token-split))
}
\description{
Token-based Jaro-Winkler matching (same algorithm as
`.get_fuzzy_matches()` in `mod_lookup_matcher`, which is what
`launch_import_wizard()` uses). Handles multi-word collector names
like "Transect Cameroun" gracefully.
}
\keyword{internal}
