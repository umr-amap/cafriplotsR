% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backbone_core.R
\name{save_backbone_links}
\alias{save_backbone_links}
\title{Save links between internal taxa and a backbone}
\usage{
save_backbone_links(
  matches,
  backbone,
  con_taxa = NULL,
  replace = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{matches}{Data frame with \code{idtax_n}, \code{external_id},
\code{match_type} and optionally \code{match_score}, e.g. from
[match_taxa_to_backbone()].}

\item{backbone}{Character. Backbone code.}

\item{con_taxa}{Connection or pool to the taxa database, with write access.}

\item{replace}{Logical. If \code{TRUE} (default), the backbone's existing
links of the taxa in \code{matches} are deleted first.}

\item{verbose}{Logical. Show progress. Default \code{TRUE}.}
}
\value{
Invisible integer: number of links written.
}
\description{
Writes matches to \code{taxa_backbone_link}. An existing link (same taxon,
backbone and external ID) has its match details updated; its
\code{verified} and \code{is_preferred} flags are kept.

A taxon left with a single link to the backbone gets that link marked
preferred. A taxon with several links gets none: it keeps its internal name
until one is chosen.
}
\examples{
\dontrun{
matches <- match_taxa_to_backbone("wcvp", con_taxa)
save_backbone_links(matches, "wcvp", con_taxa)
}

}
