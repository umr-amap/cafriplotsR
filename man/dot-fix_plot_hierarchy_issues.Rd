% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hierarchy_consistency.R
\name{.fix_plot_hierarchy_issues}
\alias{.fix_plot_hierarchy_issues}
\title{Repair the unambiguous plot hierarchy issues}
\usage{
.fix_plot_hierarchy_issues(con, issues, force = FALSE)
}
\arguments{
\item{con}{Raw database connection (not a pool)}

\item{issues}{Named list of data frames, fixable types only}

\item{force}{Logical, skip the confirmation prompt}
}
\value{
Named list of row counts, or NULL if the user declined
}
\description{
Each repair here has exactly one sensible outcome, which is why it is
allowed to run unattended:
}
\details{
- a self-parent and a parent that does not exist are both links that say
  nothing, so the link is cleared (both columns, to keep the pairing);
- a relation with no parent describes a relationship that is not there, so
  the relation is cleared.

Everything else - a parent with no relation, an unknown relation, a cycle -
needs someone who knows the plots.
}
\keyword{internal}
