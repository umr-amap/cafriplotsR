% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_column_mapping.R
\name{.column_description_box}
\alias{.column_description_box}
\title{Render the explanation shown under a mapping dropdown}
\usage{
.column_description_box(info, i18n = NULL)
}
\arguments{
\item{info}{One element of the list returned by [.get_column_descriptions()],
or `NULL` for an unmapped column.}

\item{i18n}{Optional translator object. Labels fall back to English without
one; the description text itself comes from the database and is never
translated.}
}
\value{
A `shiny.tag`, or `NULL`.
}
\description{
Turns one entry of [.get_column_descriptions()] into the small panel that
tells the user what the column they just picked actually holds. Returns
`NULL` when there is nothing to say, so callers can drop it in
unconditionally.
}
\keyword{internal}
