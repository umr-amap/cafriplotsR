% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openforis_processing.R
\name{.prepare_openforis_specimens}
\alias{.prepare_openforis_specimens}
\title{Prepare specimen list from OpenForis tree data}
\usage{
.prepare_openforis_specimens(
  trees,
  census_metadata = NULL,
  specimen_prefix = NULL,
  locality = NULL,
  country = NULL,
  col_month = NULL,
  col_year = NULL,
  collector = NULL,
  additional_people = NULL,
  description_col = "stem_diameter"
)
}
\arguments{
\item{trees}{Full tree data frame (all individuals, already tag-resolved).}

\item{census_metadata}{Census metadata tibble (used to get plot coordinates
if a \code{ddlat}/\code{ddlon} column is present). Can be NULL.}

\item{specimen_prefix}{Character prefix (e.g. "PIRD"). NULL to skip.}

\item{locality}{Character. Locality string (e.g. "Mbalmayo, Centre").}

\item{country}{Character. Country name.}

\item{col_month}{Integer. Collection month.}

\item{col_year}{Integer. Collection year.}

\item{collector}{Character. Collector code (stored as \code{colnam}).}

\item{additional_people}{Character. Additional collectors.}

\item{description_col}{Column name in \code{trees} used to build description
(default "stem_diameter"). Set to NULL to skip.}
}
\value{
Data frame ready for \code{add_specimens()}, or NULL if no specimens.
}
\description{
Filters trees that have a \code{herbarium_nbe_char} value, applies the
specimen prefix, extracts a numeric collection number (\code{colnbr}),
and attaches plot coordinates and user-supplied metadata. The output
is ready for \code{add_specimens()}.
}
\keyword{internal}
