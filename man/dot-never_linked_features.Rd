% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_census_link.R
\name{.never_linked_features}
\alias{.never_linked_features}
\title{The features in a set that are never attached to a census}
\usage{
.never_linked_features(features, policy)
}
\arguments{
\item{features}{Character vector of feature (trait) names.}

\item{policy}{Named character vector as returned by [.feature_census_link()].}
}
\value{
Character vector, the subset of `features` the policy calls
  `"never"`.
}
\description{
A convenience over an already-read policy, for callers deciding what to
offer rather than what to write. An empty policy — not read yet, or
unreadable — names nothing, leaving the caller's default in place.
}
\keyword{internal}
