% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grant_lookup_permissions.R
\name{add_person_to_db}
\alias{add_person_to_db}
\title{Add a person to table_colnam using secure function}
\usage{
add_person_to_db(
  con,
  first_name,
  last_name,
  nationality = NULL,
  institute = NULL,
  contact = NULL
)
}
\arguments{
\item{con}{Database connection}

\item{first_name}{Character. Person's first name (required)}

\item{last_name}{Character. Person's last name (required)}

\item{nationality}{Character. Person's nationality (optional)}

\item{institute}{Character. Person's institute/organization (optional)}

\item{contact}{Character. Contact email or phone (optional)}
}
\value{
Integer. The ID of the newly created (or existing) person
}
\description{
Adds a new person to table_colnam using the secure add_person() PostgreSQL function.
This works even if the user doesn't have direct INSERT permission on table_colnam.

**Requires**: Database administrator must first run `setup_add_person_function()`
}
\examples{
\dontrun{
con <- call.mydb()

# Add a new person
id <- add_person_to_db(con, "John", "Doe",
                       nationality = "USA",
                       institute = "University XYZ")

# Person already exists - returns existing ID
id2 <- add_person_to_db(con, "John", "Doe")  # Returns same ID
}

}
