% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/growth_census_functions.R
\name{compute_growth}
\alias{compute_growth}
\title{Compute growth rates for permanent plots}
\usage{
compute_growth(
  plot_ids = NULL,
  plot_names = NULL,
  mindbh = 100,
  err.limit = 4,
  maxgrow = 75,
  method = c("I", "E"),
  return_individual = TRUE,
  con = NULL
)
}
\arguments{
\item{plot_ids}{Integer vector. Plot IDs to analyze.}

\item{plot_names}{Character vector. Plot names to analyze.}

\item{mindbh}{Numeric. Minimum diameter (mm) for including measurements. Default 100.}

\item{err.limit}{Numeric. Error limit for negative growth detection. Default 4.}

\item{maxgrow}{Numeric. Maximum valid growth rate (mm/year). Default 75.}

\item{method}{Character. Growth calculation method: "I" for incremental or "E" for exponential.}

\item{return_individual}{Logical. Whether to return individual-level growth data.}

\item{con}{Database connection. If NULL, connects automatically.}
}
\value{
A list with:
  - `summary`: Data frame with growth summary per plot and census interval
  - `individuals`: Data frame with individual growth rates (if return_individual = TRUE)
}
\description{
Calculates tree growth rates between successive censuses for plots with
multiple census records. The function automatically fetches data from the
database and computes growth metrics.
}
\examples{
\dontrun{
# Compute growth for specific plots by ID
growth <- compute_growth(plot_ids = c(1, 2, 3))

# Compute growth for plots by name
growth <- compute_growth(plot_names = c("plot001", "plot002"))

# Get only summary statistics
growth <- compute_growth(plot_ids = 1, return_individual = FALSE)
}

}
