% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citations_functions.R
\name{export_plots_for_citation_backfill}
\alias{export_plots_for_citation_backfill}
\title{Export plots for citation backfill}
\usage{
export_plots_for_citation_backfill(
  con = NULL,
  file = NULL,
  only_missing = TRUE
)
}
\arguments{
\item{con}{Database connection to `plots_transects`. If NULL, calls
`call.mydb()`.}

\item{file}{Path to an `.xlsx` file to write. If NULL (default), returns
the data frame without writing.}

\item{only_missing}{Logical. If TRUE (default), export only rows where
`id_citation IS NULL`.}
}
\value{
A data frame with columns `id_liste_plots`, `plot_name`,
  `country`, `method`, `id_citation`.
}
\description{
Exports `data_liste_plots` to a data frame (optionally saved as Excel)
with a blank `id_citation` column ready to be filled in manually. Once
filled, pass the result to `apply_plot_citation_backfill()`.
}
\details{
The export contains only the columns needed to identify each plot and
assign a citation: `id_liste_plots`, `plot_name`, `country`, `method`, and
the current `id_citation` (NA where unset).
}
\examples{
\dontrun{
con <- call.mydb()

# Return as data frame
df <- export_plots_for_citation_backfill(con)

# Write to Excel for manual editing
export_plots_for_citation_backfill(con, file = "plots_to_cite.xlsx")
}

}
