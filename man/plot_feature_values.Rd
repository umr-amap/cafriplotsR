% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_feature_filters.R
\name{plot_feature_values}
\alias{plot_feature_values}
\title{Values a plot feature actually holds}
\usage{
plot_feature_values(feature, con = NULL)
}
\arguments{
\item{feature}{A single feature name, as listed by [plot_feature_filters()].}

\item{con}{Optional database connection. If `NULL`, [call.mydb()] is called.}
}
\value{
A tibble with `value` and `n_plots`, most widespread value first.
}
\description{
The distinct stored values of one filterable feature, with the number of
plots carrying each. Meant for discovering what can be asked for before
passing it to `query_plots(feature_filters = ...)`, and for populating a
dropdown in the query app.

Lookup features (`table_colnam`) are resolved to readable names, so the
values returned are the ones to filter on.
}
\examples{
\dontrun{
  plot_feature_values("data_provider", con = mydb)
  plot_feature_values("principal_investigator", con = mydb)
}

}
\seealso{
[plot_feature_filters()], [query_plots()]
}
