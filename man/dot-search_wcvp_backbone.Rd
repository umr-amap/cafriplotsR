% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_taxa_add.R
\name{.search_wcvp_backbone}
\alias{.search_wcvp_backbone}
\title{Queries the \code{wcvp_names} table (taxa database) for a given name.
First tries an exact case-insensitive match on \code{taxon_name}; if
nothing is found, falls back to a genus-level filter with a prefix
match on \code{species}, annotating rows with a \code{match_type}
column (\code{"exact"} or \code{"fuzzy"}).}
\usage{
.search_wcvp_backbone(name, con_taxa)
}
\arguments{
\item{name}{Character. Scientific name to search (e.g. \code{"Gilbertiodendron dewevrei"}).}

\item{con_taxa}{Database connection (or Pool) to the taxa database.}
}
\value{
A data frame of matching WCVP records with an extra
  \code{match_type} column, or \code{NULL} if the table does not exist.
}
\description{
Returns \code{NULL} silently if the \code{wcvp_names} table is absent.
}
\keyword{internal}
