% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_feat_step3_census_import.R
\name{.census_feature_guess}
\alias{.census_feature_guess}
\title{Guess an individual feature for each unclaimed column}
\usage{
.census_feature_guess(free_cols, traits)
}
\arguments{
\item{free_cols}{Character vector of column names still unclaimed.}

\item{traits}{Trait table from `traitlist`.}
}
\value{
Named character vector, column name to feature name, `""` when no
  guess was found.
}
\description{
Exact name first, then the synonym dictionaries. The individual-feature
synonyms matter most here: they are what sends a `subplot` or
`sous_parcelle` column to the `quadrat` feature now that `sous_plot_name`
is gone. Keys that are not real features are dropped, so a dictionary
entry such as `census_id` cannot produce an unmappable guess.
}
\keyword{internal}
