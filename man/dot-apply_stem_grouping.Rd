% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_individuals_with_transactions.R
\name{.apply_stem_grouping}
\alias{.apply_stem_grouping}
\title{Resolve multi_tiges_id into stem_grouping (Internal)}
\usage{
.apply_stem_grouping(
  individuals_data,
  individuals_id_data,
  con,
  progress = TRUE
)
}
\arguments{
\item{individuals_data}{Data frame carrying plot_name and multi_tiges_id,
in the order it was inserted.}

\item{individuals_id_data}{Data frame returned by the INSERT, with
id_individuals, tag and plot_name.}

\item{con}{Database connection (inside the open transaction).}

\item{progress}{Show progress messages.}
}
\value{
Number of stems whose stem_grouping was set.
}
\description{
\code{multi_tiges_id} is a staging column holding the \strong{tag} of the
main stem, while \code{data_individuals.stem_grouping} stores that stem's
\code{id_n}. Once the batch is inserted every \code{id_n} is known, so the
tags are resolved here — against this batch first, then against individuals
already in the plot — and written in a single UPDATE inside the same
transaction.
}
\details{
Rows whose parent cannot be resolved are left ungrouped and reported;
\code{\link{validate_individual_data}} flags them as errors beforehand.
}
\keyword{internal}
