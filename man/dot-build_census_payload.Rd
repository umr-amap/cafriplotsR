% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_feat_step3_census_import.R
\name{.build_census_payload}
\alias{.build_census_payload}
\title{Turn a classified census table into measurements plus recruits}
\usage{
.build_census_payload(
  split,
  plots,
  traits,
  trait_mapping,
  include_review = FALSE,
  census_mode = "create",
  census_number = NA,
  census_year = NA,
  census_month = NA,
  census_day = NA,
  census_map = NULL
)
}
\arguments{
\item{split}{A `census_split` from [split_census_table()].}

\item{plots}{Selected plots, with `plot_name` and `id_liste_plots`.}

\item{traits}{Trait table from `traitlist`.}

\item{trait_mapping}{Named character vector, column to trait name.}

\item{include_review}{Treat reviewed rows as recruits?}

\item{census_mode}{`"create"` or `"existing"`.}

\item{census_number, census_year, census_month, census_day}{Census identity
when creating one.}

\item{census_map}{Census map when reusing one.}
}
\value{
List with `data` (long measurements) and `config`.
}
\description{
The wide table becomes one measurement row per stem per trait, and the
recruit rows become a frame shaped for `data_individuals`. Rows held for
review are left out unless `include_review` says otherwise.
}
\keyword{internal}
