% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomic_matching.R
\name{.trigram_sim_lc}
\alias{.trigram_sim_lc}
\title{`.trigram_sim()` for an `x` that is already lowercase}
\usage{
.trigram_sim_lc(x_lc, y)
}
\arguments{
\item{x_lc}{Lowercase character vector}

\item{y}{Single character string to compare each `x_lc` against}
}
\description{
Lowercasing 365,000 backbone names costs ~0.4 s, and they are the same
names on every call. The index stores them lowered once; this skips the
redundant pass. Same result as `.trigram_sim(x, y)` when `x` is lowercase.
}
\keyword{internal}
