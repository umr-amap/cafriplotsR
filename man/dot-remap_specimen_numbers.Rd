% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/openforis_processing.R
\name{.remap_specimen_numbers}
\alias{.remap_specimen_numbers}
\title{Renumber the specimens of an OpenForis export from a correspondence table}
\usage{
.remap_specimen_numbers(trees, remap_file, data_dir = NULL)
}
\arguments{
\item{trees}{Normalised tree data frame.}

\item{remap_file}{Path to the xlsx, or a bare filename to look up in
\code{data_dir}. The first column holds the number as recorded, the second
its replacement; any further columns are ignored.}

\item{data_dir}{Directory a bare filename is resolved against. NULL to
require a full path.}
}
\value{
\code{trees} with the substitutions applied.
}
\description{
A field team numbers its vouchers from 1 and only later learns which block
of the herbarium series it was allotted, so a whole mission is renumbered in
one go from a two-column table. Both the specimen number and the herbarium
code are substituted, and the values as recorded are kept in
\code{specimen_number_original} and \code{herbarium_nbe_char_original}.
}
\keyword{internal}
