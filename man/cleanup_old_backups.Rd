% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_backup_functions.R
\name{cleanup_old_backups}
\alias{cleanup_old_backups}
\title{Delete old database backups}
\usage{
cleanup_old_backups(
  backup_dir = "~/database_backups",
  days_to_keep = 30,
  database = c("all", "main", "taxa"),
  dry_run = TRUE
)
}
\arguments{
\item{backup_dir}{Directory containing backup files. Defaults to `~/database_backups`.}

\item{days_to_keep}{Number of days to keep backups. Backups older than this will be deleted.
Default is 30 days.}

\item{database}{Filter by database: `"main"`, `"taxa"`, or `"all"`.
Default is `"all"`.}

\item{dry_run}{If TRUE, shows what would be deleted without actually deleting.
Default is TRUE.}
}
\value{
Integer. Number of files deleted (or that would be deleted in dry_run mode).
}
\description{
Removes backup files older than a specified number of days.
}
\examples{
\dontrun{
# Preview what would be deleted (dry run)
cleanup_old_backups(days_to_keep = 30, dry_run = TRUE)

# Actually delete old backups
cleanup_old_backups(days_to_keep = 30, dry_run = FALSE)

# Delete only main database backups older than 7 days
cleanup_old_backups(database = "main", days_to_keep = 7, dry_run = FALSE)
}

}
