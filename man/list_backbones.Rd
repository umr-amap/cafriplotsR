% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backbone_core.R
\name{list_backbones}
\alias{list_backbones}
\title{List the taxonomic backbones available in the taxa database}
\usage{
list_backbones(con_taxa = NULL, name_sources_only = TRUE)
}
\arguments{
\item{con_taxa}{Connection or pool to the taxa database. If \code{NULL},
calls \code{call.mydb.taxa()}.}

\item{name_sources_only}{Logical. If \code{TRUE} (default), only backbones
offered to users as a source of names (\code{is_name_source}).}
}
\value{
A tibble with columns \code{id_backbone}, \code{code}, \code{name},
  \code{publisher}, \code{names_view}, \code{url_template},
  \code{is_name_source}.
}
\description{
The internal backbone (\code{table_taxa}) is always available and is not
listed. Other backbones are registered in \code{backbone_list}, each with a
mirror of its names exposed through a view.

If \code{backbone_list} cannot be read (database not migrated, no
permission), an empty tibble is returned and only the internal backbone is
usable.
}
\examples{
\dontrun{
list_backbones()
}

}
