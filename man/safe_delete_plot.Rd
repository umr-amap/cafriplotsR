% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_delete_plot.R
\name{safe_delete_plot}
\alias{safe_delete_plot}
\title{Safely delete plot(s) with all related data}
\usage{
safe_delete_plot(
  plot_ids,
  con = NULL,
  dry_run = TRUE,
  force = FALSE,
  delete_individuals = TRUE,
  delete_subplots = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{plot_ids}{Integer vector. Plot ID(s) to delete (id_liste_plots)}

\item{con}{Database connection. If NULL, will connect automatically.}

\item{dry_run}{Logical. If TRUE, shows what would be deleted without deleting.
Default TRUE for safety.}

\item{force}{Logical. If TRUE, skips confirmation prompts. Default FALSE.
**USE WITH EXTREME CAUTION!**}

\item{delete_individuals}{Logical. Delete individuals? Default TRUE.}

\item{delete_subplots}{Logical. Delete subplot features? Default TRUE.}

\item{verbose}{Logical. Show detailed progress? Default TRUE.}
}
\value{
List with deletion summary (invisible)
}
\description{
**DANGER: This permanently deletes data from the database!**

This function safely deletes one or more plots and all related data in the
correct order to respect foreign key constraints:
1. Individual measurement features
2. Trait measurements
3. Individual measurements (if exists)
4. Individuals
5. Subplot features
6. Subplots
7. Plot

**Safety features:**
- Dry-run mode to preview what will be deleted
- Shows counts of all related data
- Requires explicit confirmation (unless force = TRUE)
- Uses database transaction (rolls back on error)
- Detailed logging of each step
}
\examples{
\dontrun{
con <- call.mydb()

# STEP 1: Always do dry-run first!
safe_delete_plot(plot_ids = 123, dry_run = TRUE)

# STEP 2: Review the output, then delete if sure
safe_delete_plot(plot_ids = 123, dry_run = FALSE)

# Delete multiple plots
safe_delete_plot(plot_ids = c(123, 124, 125))

# Delete plot but keep individuals (rare)
safe_delete_plot(plot_ids = 123, delete_individuals = FALSE)
}

}
