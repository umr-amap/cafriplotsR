% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backbone_core.R
\name{check_backbone_links}
\alias{check_backbone_links}
\title{Check the links between internal taxa and a backbone}
\usage{
check_backbone_links(backbone, con_taxa = NULL, max_depth = 5L, verbose = TRUE)
}
\arguments{
\item{backbone}{Character. Backbone code.}

\item{con_taxa}{Connection or pool to the taxa database. If \code{NULL},
calls \code{call.mydb.taxa()}.}

\item{max_depth}{Integer. Maximum number of synonym steps followed.
Default 5.}

\item{verbose}{Logical. Print the counts. Default \code{TRUE}.}
}
\value{
Invisibly, a tibble with columns \code{check} and \code{n}.
}
\description{
Read-only. Counts what keeps taxa from getting a backbone's names:
\itemize{
  \item taxa with several links and none preferred (they keep their
    internal name);
  \item links to an ID absent from the backbone, e.g. after an import
    removed it;
  \item preferred links whose synonym chain cannot be completed (missing
    target, cycle, more than \code{max_depth} steps);
  \item fuzzy links not yet verified.
}
}
\examples{
\dontrun{
check_backbone_links("wcvp")
}

}
