% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_management.R
\name{register_user}
\alias{register_user}
\title{Register a user in the registry}
\usage{
register_user(
  con,
  username,
  email = NULL,
  first_name = NULL,
  last_name = NULL,
  institution = NULL,
  notes = NULL
)
}
\arguments{
\item{con}{Connection to main database.}

\item{username}{Character. PostgreSQL username (must match the role name).}

\item{email}{Character. User's email address.}

\item{first_name}{Character. User's first name (optional).}

\item{last_name}{Character. User's last name (optional).}

\item{institution}{Character. User's institution (optional).}

\item{notes}{Character. Additional notes (optional).}
}
\value{
TRUE if successful, FALSE otherwise.
}
\description{
Adds or updates a user's metadata in the `user_registry` table.
The user must already exist as a PostgreSQL role (created via OVH portal).
}
\examples{
\dontrun{
con <- call.mydb()
register_user(con,
  username = "jdupont",
  email = "j.dupont@institution.org",
  first_name = "Jean",
  last_name = "Dupont",
  institution = "IRD"
)
}

}
