% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_monitoring.R
\name{setup_db_activity_scheduler}
\alias{setup_db_activity_scheduler}
\title{Register the 30-minute recording script in Windows Task Scheduler}
\usage{
setup_db_activity_scheduler(
  log_dir,
  db_user,
  db_password,
  task_name = "CafriplotsR_db_monitor",
  rscript_exe = NULL
)
}
\arguments{
\item{log_dir}{Directory where the CSV logs will be written.}

\item{db_user}{Database username (stored as a Task Scheduler env var).}

\item{db_password}{Database password (stored as a Task Scheduler env var).}

\item{task_name}{Name for the scheduled task.  Defaults to
\code{"CafriplotsR_db_monitor"}.}

\item{rscript_exe}{Path to \file{Rscript.exe}.  Auto-detected by default.}
}
\value{
Invisible \code{TRUE} on success.
}
\description{
Creates a Windows Task Scheduler task that runs
\file{record_db_activity.R} every 30 minutes, passing database credentials
as environment variables.
}
\details{
The task is created via \code{schtasks.exe} (Windows only).  Run once
from an R session with administrator privileges, or add the task manually
using the Windows Task Scheduler GUI with the printed command.
}
\examples{
\dontrun{
setup_db_activity_scheduler(
  log_dir     = "C:/db_logs/cafri",
  db_user     = "your_username",
  db_password = "your_password"
)
}
}
