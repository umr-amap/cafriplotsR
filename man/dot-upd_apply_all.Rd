% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update_app_resolver.R
\name{.upd_apply_all}
\alias{.upd_apply_all}
\title{Write a record's flat columns and its feature records as one unit}
\usage{
.upd_apply_all(entity, id, values, features, con, link = NULL)
}
\arguments{
\item{entity}{`"plot"` or `"individual"`.}

\item{id}{The record id.}

\item{values}{Named list of database column -> new value for the flat table.}

\item{features}{Named list keyed by feature record id (see
[.upd_apply_feature()]).}

\item{con}{A DBI connection.}

\item{link}{Plots only: `NULL`, or a list with `id_parent_plot` and
`parent_relation` to write as the plot's parent link.}
}
\value{
A list with `n_direct`, `n_feature` and `n_link`: how many values
  were written.
}
\description{
Flat columns and features live in different tables, so applying them
separately can leave a record half-updated if the second write fails. Both
go inside one transaction; anything raised rolls the whole edit back. For a
plot the parent link joins them, written by [.upd_apply_plot_link()] rather
than through `values` - see [.upd_entity_spec()] for why it cannot go the
ordinary way.
}
\keyword{internal}
