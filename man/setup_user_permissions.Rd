% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user_management.R
\name{setup_user_permissions}
\alias{setup_user_permissions}
\title{Setup user permissions on both databases}
\usage{
setup_user_permissions(
  con_main,
  con_taxa = NULL,
  username,
  email = NULL,
  first_name = NULL,
  last_name = NULL,
  institution = NULL,
  main_db_access = c("read_write", "read_only", "none"),
  taxa_db_access = c("read_only", "read_write", "none"),
  plot_ids = NULL,
  notes = NULL
)
}
\arguments{
\item{con_main}{Connection to main database.}

\item{con_taxa}{Connection to taxa database (optional).}

\item{username}{Character. PostgreSQL username.}

\item{email}{Character. User's email address (optional but recommended).}

\item{first_name}{Character. User's first name (optional).}

\item{last_name}{Character. User's last name (optional).}

\item{institution}{Character. User's institution (optional).}

\item{main_db_access}{Character. Access level for plots_transects:
`"read_only"`, `"read_write"`, or `"none"`. Default `"read_write"`.}

\item{taxa_db_access}{Character. Access level for rainbio:
`"read_only"`, `"read_write"`, or `"none"`. Default `"read_only"`.}

\item{plot_ids}{Integer vector. Plot IDs to grant access to (optional).
If provided, RLS policies will be set for these plots.}

\item{notes}{Character. Additional notes about the user (optional).}
}
\value{
A list with setup results.
}
\description{
One-command setup to grant a user appropriate permissions on the main database
(`plots_transects`) and/or the taxa database (`rainbio`). Also registers the
user in the `user_registry` table.

The user must already exist as a PostgreSQL role (created via OVH portal).

**For database administrators only.**
}
\examples{
\dontrun{
con <- call.mydb()
con_taxa <- call.mydb.taxa()

# Full setup for a new user
setup_user_permissions(
  con_main = con,
  con_taxa = con_taxa,
  username = "jdupont",
  email = "j.dupont@institution.org",
  first_name = "Jean",
  last_name = "Dupont",
  institution = "IRD",
  main_db_access = "read_write",
  taxa_db_access = "read_only",
  plot_ids = c(10, 15, 22)
)
}

}
