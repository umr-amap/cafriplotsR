% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_monitoring.R
\name{monitor_db}
\alias{monitor_db}
\title{Collect, print, and save a database activity report in one call}
\usage{
monitor_db(con, output_dir = ".", open = TRUE)
}
\arguments{
\item{con}{A database connection from \code{\link{call.mydb}}.}

\item{output_dir}{Directory for the HTML report (passed to
\code{\link{save_db_activity_report}}). Set to \code{NULL} to skip
saving.}

\item{open}{Logical. Open the HTML report in the browser? Default
\code{TRUE}.}
}
\value{
A \code{db_activity} object (invisibly).
}
\description{
Convenience wrapper that calls \code{\link{get_db_activity}},
\code{\link{print.db_activity}}, and \code{\link{save_db_activity_report}}.
}
\examples{
\dontrun{
con <- call.mydb()
monitor_db(con)

# Save to a specific folder without opening browser
monitor_db(con, output_dir = "~/db_reports", open = FALSE)
}
}
