% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database_monitoring.R
\name{log_db_snapshot}
\alias{log_db_snapshot}
\title{Append a 30-minute activity snapshot to the log files}
\usage{
log_db_snapshot(con, log_dir, verbose = TRUE)
}
\arguments{
\item{con}{A database connection from \code{\link{call.mydb}}.}

\item{log_dir}{Directory containing the log files.  Will be initialised
with \code{\link{init_activity_log}} if it does not exist yet.}

\item{verbose}{Logical.  Print a one-line confirmation?  Default
\code{TRUE}.}
}
\value{
The \code{db_activity} snapshot (invisibly).
}
\description{
Calls \code{\link{get_db_activity}} and appends one row per active
connection to \file{connections_log.csv} and one row per database to
\file{stats_log.csv}.  Designed to be called on a regular schedule
(e.g. every 30 minutes via Windows Task Scheduler).
}
\examples{
\dontrun{
con <- call.mydb()
log_db_snapshot(con, log_dir = "~/db_logs")
}
}
