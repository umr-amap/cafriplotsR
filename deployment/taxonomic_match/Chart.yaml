apiVersion: v2
name: cafri-taxonomic-match
description: Taxonomic Name Standardization Shiny app (CafriplotsR) on SSP Cloud
type: application
version: 0.1.0
appVersion: "1.9.92"

# Inherit SSP Cloud's generic Shiny chart. Run `helm dependency update` in this
# directory to fetch it into charts/ before installing.
dependencies:
  - name: shiny
    version: 1.x.x
    repository: https://inseefrlab.github.io/helm-charts-interactive-services
