apiVersion: v2
name: cafri-taxonomic-match
description: Taxonomic Name Standardization Shiny app (CafriplotsR) on SSP Cloud
type: application
version: 0.1.0
appVersion: "1.9.92"

# Inherit SSP Cloud's generic Shiny chart (InseeFrLab helm-charts repo).
# Run `helm dependency update` in this directory to fetch it into charts/.
dependencies:
  - name: shiny
    version: 0.17.0
    repository: https://inseefrlab.github.io/helm-charts
