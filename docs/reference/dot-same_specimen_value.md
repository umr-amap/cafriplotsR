# Compare a current and a new specimen value

Compare a current and a new specimen value

## Usage

``` r
.same_specimen_value(current, new)
```

## Arguments

- current:

  Current value.

- new:

  New value (already coerced).

## Value

Logical, `TRUE` when both values are equivalent (including both being
missing).
