# Choices for a lookup column, as a named vector (label -\> id)

Choices for a lookup column, as a named vector (label -\> id)

## Usage

``` r
.upd_lookup_choices(lookup_table, lookup_key, lookup_value, con)
```
