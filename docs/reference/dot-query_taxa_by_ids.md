# Query taxa by IDs (internal helper)

Query taxa by IDs (internal helper)

## Usage

``` r
.query_taxa_by_ids(
  ids,
  class,
  mydb_taxa,
  only_genus,
  only_family,
  only_class,
  check_synonymy,
  extract_traits,
  include_children = FALSE,
  verbose
)
```
