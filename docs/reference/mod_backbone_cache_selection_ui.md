# Backbone Cache Selection Module - UI

UI component for backbone cache selection modal. The modal is created
dynamically in the server, so this returns an empty div.

## Usage

``` r
mod_backbone_cache_selection_ui(id)
```

## Arguments

- id:

  Character, module namespace ID

## Value

Shiny UI element
