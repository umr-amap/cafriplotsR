# Read the progress published by a matching worker

Read the progress published by a matching worker

## Usage

``` r
.read_matching_progress(progress_file)
```

## Arguments

- progress_file:

  Character path.

## Value

A list with \`stage\`, \`i\`, \`n\`, \`name\`, or NULL if nothing
readable.
