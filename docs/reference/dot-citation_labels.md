# One-line label for each citation

One-line label for each citation

## Usage

``` r
.citation_labels(df)
```

## Arguments

- df:

  Data frame with \`citation_key\`, \`authors\`, \`year\` and
  \`dataset_name\`.

## Value

Character vector, one label per row.
