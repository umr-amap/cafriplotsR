# Get Column Descriptions from Database (Internal Helper)

Returns descriptions for template columns, pulling dynamically from
database via subplot_list() for subplot features. No hardcoding of
feature lists.

## Usage

``` r
.get_column_descriptions_from_db()
```

## Value

Named list with column descriptions and validation info
