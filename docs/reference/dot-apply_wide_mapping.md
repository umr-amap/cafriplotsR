# Apply wide format mapping and build prepared data

Apply wide format mapping and build prepared data

## Usage

``` r
.apply_wide_mapping(
  df,
  traits,
  census_map,
  input,
  ns,
  i18n,
  link_policy = character(0)
)
```
