# Diagnose add_person function and permissions

Diagnostic tool to check if the secure add_person() function is properly
set up and accessible by the current user.

## Usage

``` r
diagnose_add_person_setup(con, verbose = TRUE)
```

## Arguments

- con:

  Database connection

- verbose:

  Logical. Print detailed information? Default TRUE.

## Value

List with diagnostic information
