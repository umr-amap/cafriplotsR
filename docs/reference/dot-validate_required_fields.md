# Validate Required Fields

Checks that all required columns are present and non-empty.

## Usage

``` r
.validate_required_fields(data, required_columns)
```

## Arguments

- data:

  Data frame with schema column names

- required_columns:

  Character vector of required column names

## Value

List of error objects
