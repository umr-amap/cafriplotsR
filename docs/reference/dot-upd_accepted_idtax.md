# Accepted id for each taxon id, through \`table_idtax\` synonymy

Accepted id for each taxon id, through \`table_idtax\` synonymy

## Usage

``` r
.upd_accepted_idtax(idtax, con)
```

## Value

Named integer vector, idtax as character -\> accepted idtax. Ids the
table does not know map to themselves.
