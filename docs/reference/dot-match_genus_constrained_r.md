# Genus-constrained fuzzy match against cached backbone

Genus-constrained fuzzy match against cached backbone

## Usage

``` r
.match_genus_constrained_r(
  parsed,
  backbone,
  min_similarity,
  include_authors,
  max_matches
)
```
