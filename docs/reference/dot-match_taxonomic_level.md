# Match taxonomic level (order/family/genus) using new matching functions

Match taxonomic level (order/family/genus) using new matching functions

## Usage

``` r
.match_taxonomic_level(
  names,
  level,
  field,
  exact_match,
  min_similarity,
  mydb_taxa,
  verbose
)
```
