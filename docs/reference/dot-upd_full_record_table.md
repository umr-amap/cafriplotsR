# Pick the table holding the record out of a query_plots() result

\`query_plots()\` returns a list of tables, or a bare data frame when
only one table came back.

## Usage

``` r
.upd_full_record_table(res, entity)
```
