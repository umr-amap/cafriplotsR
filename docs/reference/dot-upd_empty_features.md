# Empty feature table with the documented shape

Empty feature table with the documented shape

## Usage

``` r
.upd_empty_features(with_plot = FALSE)
```

## Arguments

- with_plot:

  Whether to carry the plot resolver's \`id_plot\` and \`plot_name\`
  columns, so a zero-row result has the same shape as a populated one.
