# Batch mapping server

Batch mapping server

## Usage

``` r
mod_specid_batch_mapping_server(id, data, i18n)
```

## Arguments

- id:

  Module id

- data:

  Reactive uploaded data frame

- i18n:

  Reactive translator

## Value

list(mappings = reactive(named list), is_valid = reactive(logical))
