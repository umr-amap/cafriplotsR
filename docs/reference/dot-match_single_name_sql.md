# Match a single parsed name using SQL-side queries (internal helper)

Match a single parsed name using SQL-side queries (internal helper)

## Usage

``` r
.match_single_name_sql(
  parsed,
  con,
  method,
  max_matches,
  min_similarity,
  include_authors,
  verbose
)
```
