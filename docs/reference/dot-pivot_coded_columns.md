# Pivot coded columns to long format and decode via code list

Pivot coded columns to long format and decode via code list

## Usage

``` r
.pivot_coded_columns(
  data,
  coded_cols,
  code_list,
  code_col = "item_code",
  label_col = "item_label_en"
)
```
