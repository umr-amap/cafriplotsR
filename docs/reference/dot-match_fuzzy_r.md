# Full fuzzy match against cached backbone (last resort)

Full fuzzy match against cached backbone (last resort)

## Usage

``` r
.match_fuzzy_r(parsed, backbone, min_similarity, include_authors, max_matches)
```
