# Numeric value of a tag, or \`NA\` when it is not a plain number

Numeric value of a tag, or \`NA\` when it is not a plain number

## Usage

``` r
.tag_numeric(x)
```

## Arguments

- x:

  Character vector of normalised tags.

## Value

Numeric vector.
