# Enrich Preview Data with Lookup Names

Replaces IDs with readable names for lookup columns (method, country,
people) for better user experience in preview

## Usage

``` r
.enrich_preview_with_lookup_names(data)
```

## Arguments

- data:

  Data frame with schema column names

## Value

Data frame with IDs replaced by names
