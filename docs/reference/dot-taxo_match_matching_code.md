# Build the \`standardize_taxonomic_batch()\` code

Build the \`standardize_taxonomic_batch()\` code

## Usage

``` r
.taxo_match_matching_code(params)
```

## Arguments

- params:

  Settings captured at the last matching run
