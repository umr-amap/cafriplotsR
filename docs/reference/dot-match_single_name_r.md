# Match a single parsed name using R-side queries on cached backbone

Match a single parsed name using R-side queries on cached backbone

## Usage

``` r
.match_single_name_r(
  parsed,
  backbone,
  method,
  max_matches,
  min_similarity,
  include_authors,
  verbose
)
```
