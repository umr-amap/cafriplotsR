# Full fuzzy matching helper using SQL SIMILARITY (last resort)

Full fuzzy matching helper using SQL SIMILARITY (last resort)

## Usage

``` r
.match_fuzzy_sql(parsed, con, min_similarity, include_authors, max_matches)
```
