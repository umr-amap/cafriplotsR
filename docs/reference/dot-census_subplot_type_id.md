# Resolve the \`census\` subplot type id

Resolve the \`census\` subplot type id

## Usage

``` r
.census_subplot_type_id(con)
```

## Arguments

- con:

  Database connection.

## Value

Integer id from \`subplotype_list\`.
