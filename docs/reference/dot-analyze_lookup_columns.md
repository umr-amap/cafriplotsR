# Analyze Lookup Columns in User Data

Checks which lookup values exist in database (exact match) and which
need matching

## Usage

``` r
.analyze_lookup_columns(data, mappings, con, config = NULL)
```

## Arguments

- data:

  User data frame

- mappings:

  Column mappings

- con:

  Database connection

## Value

List with analysis results
