# Execute multi-stem grouping import

Resolves tag + plot_name to id_n, then updates stem_grouping via
update_records().

## Usage

``` r
.execute_multi_stems_import(data, con, dry_run = TRUE, i18n = NULL)
```
