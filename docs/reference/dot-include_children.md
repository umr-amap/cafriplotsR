# Recursively include all child taxa via id_parent

Recursively include all child taxa via id_parent

## Usage

``` r
.include_children(res, mydb_taxa, verbose)
```

## Arguments

- res:

  data.frame of initial taxa results

- mydb_taxa:

  database connection

- verbose:

  logical

## Value

data.frame with children appended
