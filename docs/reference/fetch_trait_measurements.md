# Fetch trait measurements with automatic chunking

Fetch trait measurements with automatic chunking

## Usage

``` r
fetch_trait_measurements(
  individual_ids,
  trait_ids,
  id_trait_measures = NULL,
  con
)
```
