# Simple auto-matching of user columns to known column names via synonyms

Simple auto-matching of user columns to known column names via synonyms

## Usage

``` r
.auto_match_columns(user_cols, synonyms)
```
