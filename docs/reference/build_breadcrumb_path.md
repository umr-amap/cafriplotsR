# Build Breadcrumb Path HTML

Creates a breadcrumb-style path showing the hierarchy.

## Usage

``` r
build_breadcrumb_path(hierarchy, i18n)
```

## Arguments

- hierarchy:

  Hierarchy list from get_taxon_hierarchy

- i18n:

  i18n translator

## Value

Shiny tagList
