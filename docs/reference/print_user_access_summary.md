# Print user access summary

Prints a formatted summary of user access to the database.

## Usage

``` r
print_user_access_summary(con = NULL)
```

## Arguments

- con:

  Connection to main database (optional).
