# Add a row identifier column when the data has none

Add a row identifier column when the data has none

## Usage

``` r
.add_id_data(data)
```

## Arguments

- data:

  data.frame.

## Value

\`data\` with an \`id_data\` column.
