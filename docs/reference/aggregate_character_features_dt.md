# Aggregate character features using data.table

Aggregate character features using data.table

## Usage

``` r
aggregate_character_features_dt(
  data,
  include_census,
  mode,
  include_issue = FALSE,
  include_measurement_ids = FALSE,
  census_strategy = c("last", "first", "mean")
)
```
