# One row per feature: what the extracted table shows, and from how many records

One row per feature: what the extracted table shows, and from how many
records

## Usage

``` r
.upd_feature_summary(records)
```
