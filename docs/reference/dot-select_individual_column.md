# Select Individual Column Interactively - Internal

Uses .find_cat() to let user select which individual column this
represents from a numbered list.

## Usage

``` r
.select_individual_column(column_name)
```

## Arguments

- column_name:

  Name of the column being mapped

## Value

Character: selected individual column name, or NA if skipped
