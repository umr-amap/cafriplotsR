# Validate Column Types

Checks that column values match expected data types from database.

## Usage

``` r
.validate_column_types(data, config, con)
```

## Arguments

- data:

  Data frame with schema column names

- config:

  Routing configuration

- con:

  Database connection

## Value

List with errors and warnings
